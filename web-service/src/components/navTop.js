import React from 'react';

export default (props) => (
  <section className="landing-page-two">
    <div className="text-center title">
      <p>{props.title}</p>
    </div>
    <div className="overlay-two" />
  </section>
);
